# P1 课程 Manifest 与单集探针报告

日期：2026-08-25

状态：50 集来源账本已完成；单集媒体处理探针尚未通过，P2 保持关闭

## 1. 课程来源账本

- 主学派：黑辉极乐鸟
- 课程：极乐鸟的建筑课堂
- 平台课程 ID：`4369851`
- 公开课程入口：<https://www.bilibili.com/video/BV1HhEuzZEyZ/>
- 采集集数：50
- 唯一 BV 号：50
- 总时长：51,992 秒，约 14.44 小时
- 原始响应：仅保存在 `.local/architecture-playbook/`，未提交
- 结构化账本：`docs/architecture-playbook/course/course-manifest.json`

账本按课程源顺序保留课程标题、实际发布标题、BV/AV/CID、时长、发布时间、公开状态、元数据指纹和处理状态。播放量、点赞、评论等易变统计没有进入单集指纹。

权利字段只记录平台观察，不解释为许可证。目标探针集的 `download` 和 `no_reprint` 位来自直接视频响应；合集摘要中的其余 49 集权利位与直接响应存在不一致，因此全部保持 `unknown`。所有源媒体、字幕、截图和衍生训练用途的对外发布状态仍为 `not-authorized`。

## 2. 固定技术探针

- 课程位置：`1.2 结构主次`
- BV：`BV1HhEuzZEyZ`
- CID：`29903029909`
- 课程时长：781 秒
- 直接发布标题：还在做这样的火柴盒？结构主次很重要！

### 当前能力状态

| 检查项 | 状态 | 证据或原因 |
| --- | --- | --- |
| 稳定课程身份 | 通过 | manifest 合同验证，BV/CID/作者/课程均固定 |
| 直接视频元数据 | 通过 | 匿名公开 API 返回成功 |
| 播放清单定位 | 通过 | 匿名响应提供 360p MP4，标称 46,732,394 字节、780,555 ms |
| 媒体本地缓存 | 阻塞 | 尚未保存并计算文件哈希 |
| 平台字幕 | 阻塞 | 匿名播放器响应为 `need_login_subtitle: true`；零条可见轨不等于字幕不存在 |
| 本地自动转写 | 阻塞 | 当前环境没有 ASR 工具 |
| 关键帧提取 | 阻塞 | 当前环境没有 ffmpeg |
| 术语复核 | 阻塞 | 没有带时间戳的转写草稿 |
| 证据包重建 | 阻塞 | 转写、关键帧和术语复核前置条件未满足 |

## 3. 探针门槛判断

P1 单集探针当前不能标记为通过。已证明的只是：课程身份、公开元数据和匿名播放流定位可行。尚未证明：

1. 源媒体文件能稳定缓存并以哈希固定；
2. 自动转写能生成可复核的中文时间码；
3. 关键帧能按教学事件而非固定间隔选择；
4. 方块名、材料名、数字、比例、否定词和条件词能得到术语复核；
5. 同一媒体、配置和处理器版本能重建相同证据索引。

因此尚不生成生产规则，也不开始六集批处理。

## 4. 下一步

下一步只安装或引入忽略目录中的最小本地工具链：媒体缓存、ffmpeg 兼容解码和中文 ASR。完成后重新运行同一 BV 探针，生成媒体哈希、带时间戳转写索引、事件关键帧索引、术语未知清单和最少一个完整教学段落；全部通过后才打开 P2。
