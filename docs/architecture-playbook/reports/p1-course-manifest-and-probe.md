# P1 课程 Manifest 与单集探针报告

日期：2026-08-25

状态：50 集来源账本与单集媒体处理探针均已通过；P2 可以开始

## 1. 课程来源账本

- 主学派：黑辉极乐鸟
- 课程：极乐鸟的建筑课堂
- 平台课程 ID：`4369851`
- 公开课程入口：<https://www.bilibili.com/video/BV1HhEuzZEyZ/>
- 采集集数：50
- 唯一 BV 号：50
- 总时长：51,992 秒，约 14.44 小时
- 原始响应：仅保存在 `.local/architecture-playbook/`，未提交
- 结构化账本：`docs/architecture-playbook/course/course-manifest.json`

账本按课程源顺序保留课程标题、实际发布标题、BV/AV/CID、时长、发布时间、公开状态、元数据指纹和处理状态。播放量、点赞、评论等易变统计没有进入单集指纹。

权利字段只记录平台观察，不解释为许可证。目标探针集的 `download` 和 `no_reprint` 位来自直接视频响应；合集摘要中的其余 49 集权利位与直接响应存在不一致，因此全部保持 `unknown`。所有源媒体、字幕、截图和衍生训练用途的对外发布状态仍为 `not-authorized`。

## 2. 固定技术探针

- 课程位置：`1.2 结构主次`
- BV：`BV1HhEuzZEyZ`
- CID：`29903029909`
- 课程时长：781 秒
- 直接发布标题：还在做这样的火柴盒？结构主次很重要！

### 当前能力状态

| 检查项 | 状态 | 证据或原因 |
| --- | --- | --- |
| 稳定课程身份 | 通过 | manifest 合同验证，BV/CID/作者/课程均固定 |
| 直接视频元数据 | 通过 | 匿名公开 API 返回成功 |
| 播放清单定位 | 通过 | 匿名响应提供 360p MP4，标称 46,732,394 字节、780,555 ms |
| 媒体本地缓存 | 通过 | 46,732,394 字节；SHA-256 已固定；文件仅位于忽略目录 |
| 平台字幕 | 受限 | 匿名播放器响应为 `need_login_subtitle: true`；零条可见轨不等于字幕不存在 |
| 本地自动转写 | 通过 | `faster-whisper 1.2.1` small / CPU int8；407 个带词级时间戳的草稿段 |
| 关键帧提取 | 通过 | `PyAV 18.1.0` 解码；8 个按转写教学事件选择的帧，联系表已视觉复核 |
| 术语复核 | 通过但有未知 | 1 个核心术语已由标题与转写互证；3 个边界或方法术语保持 unresolved |
| 证据包重建 | 通过 | 2 条私有 EvidenceNote 草稿；输入和索引均固定哈希；未生成生产规则 |

## 3. 探针门槛判断

P1 单集探针达到通过门槛，已证明：

1. 源媒体文件能够缓存并以字节数和 SHA-256 固定；
2. 本地 ASR 能生成中文段落与词级时间码；
3. 能以转写中的教学转折为候选，提取并视觉复核事件关键帧；
4. 术语、否定和方法边界能够进入 resolved/unresolved 清单，而非由模型补全；
5. 一个完整教学段落能够形成同时引用时间码、画面、媒体哈希、转写索引和帧索引的 EvidenceNote 草稿；
6. 完整字幕、媒体、帧和证据草稿均留在忽略目录，Git 只保存 Schema、来源账本和原创报告。

本次通过只证明处理方法可行，不证明 ASR 文本已经忠实到可以直接升级规则。平台字幕仍需登录，当前转写是本地草稿；术语清单仍有 3 项 unresolved；2 条 EvidenceNote 只用于验证合同和重建，不进入生产规则库。

## 4. 下一步

P2 现已打开。下一步按既定六集集合处理 `1.1 结构入门`、`1.2 结构主次`、`2.1 屋顶入门`、`3.1 墙面入门`、`中世纪 1.1 民居结构` 和 `中世纪 1.2 民居范例`。每集先产生私有转写、事件帧、术语未知清单和 EvidenceNote，再允许形成带证据回链的规则候选。
